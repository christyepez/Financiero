# Financial Sprint 11 P1 Return-to-PASS Criteria

## Sprint 11 P3 review update

Sprint 11 P3 reviewed Return-to-PASS criteria and found `NoResponse` / `EvidencePending`. Preflight returned `SCRIPT_EXIT=2`; Return-to-PASS remains closed and PASS capture is NOT_READY until evidence is accepted and preflight returns `SCRIPT_EXIT=0`.

No SRI Production; No official ATS; No legal-final RIDE; No productive XAdES.

## Sprint 11 P2 review update

Sprint 11 P2 reviewed these criteria and found no accepted external owner evidence. Return-to-PASS remains closed until evidence is accepted and preflight returns `SCRIPT_EXIT=0`.

Review: `docs/qa/financial-sprint-11-p2-return-to-pass-review.md`.

No SRI Production; No official ATS; No legal-final RIDE; No productive XAdES.

Date: 2026-07-20  
Current state: `BLOCKED_DEPENDENCY`  
PASS capture state: closed until accepted evidence exists.

Return to PASS requires shared SQL, Portal Gateway and `SCRIPT_EXIT=0`; until then the system remains not production-ready.

Accepted evidence must be external owner evidence, not evidence generated by duplicated capabilities inside Financiero.

## Evidence that allows reopening PASS capture

| Gate | Required evidence | Validation |
|---|---|---|
| Gate 1 shared SQL TCP | Owner-provided sanitized TCP PASS | Preflight `Shared SQL TCP` PASS |
| Gate 2 `FinancieroDb` | Owner-provided sanitized DB availability proof | DB evidence accepted and SQL TCP PASS |
| Gate 3 Portal Gateway health | Owner-provided HTTP 2xx health/readiness route | Preflight Portal Gateway health PASS |
| Gate 4 Portal Shell health | Owner-provided HTTP 2xx Shell health route | Preflight with Shell URL/path PASS |
| Gate 5 PortalShellContext live | Sanitized live context sample | Contract accepted, no tokens/private URLs |
| Gate 6 Financiero API health | Runtime API health after dependencies are ready | `/health`, `/health/live`, `/health/ready` PASS |
| Gate 7 Portal integration readiness | Financiero readiness endpoint with Portal evidence | Readiness PASS |
| Gate 8 Preflight exit code 0 | Full sanitized preflight output outside repo | `SCRIPT_EXIT=0` |
| Gate 9 no-production guardrails | Static/documented guardrails | PASS |

## Evidence that is not sufficient

- Screenshots with hidden or cropped data.
- Logs with secrets or private URLs.
- Mock-only evidence.
- Development standalone Shell evidence.
- Evidence from a duplicated SQL/Gateway/Shell inside Financiero.
- Partial evidence that does not map to a gate.

## Required preflight command

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File tools/validate-portal-financiero-e2e.ps1 -OutputMarkdown -VerboseDiagnostics -SuggestFixes -PortalGatewayHealthPath /health -PortalShellHealthPath /health -FinancialApiHealthPath /health -EvidenceOutputPath <TEMP_PATH_OUTSIDE_REPO> -AcceptanceGateReport
```

## When Sprint 11 P2 PASS capture is allowed

Sprint 11 P2 may start only when all required owner evidence is accepted and the preflight can reasonably return 0. PASS must be documented only after actual `SCRIPT_EXIT=0`.

## When to keep BLOCKED_DEPENDENCY

- Any owner evidence is missing.
- Any gate remains blocked.
- Preflight exits 2.
- Evidence is mock-only or development-only.

## When to mark REJECTED_EVIDENCE

- Evidence contains secrets, tokens, private URLs, certificates, XML reales, personal data or real SRI responses.
- Evidence proves a duplicated SQL/Gateway/Shell/Auth capability inside Financiero.
- Evidence cannot be mapped to a gate.

No SRI Production; No official ATS; No legal-final RIDE; No productive XAdES. Financiero remains not production-ready.
