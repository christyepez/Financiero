import { Component } from '@angular/core';

@Component({
  selector: 'fin-purchase-tax-line-form',
  standalone: true,
  template: `<p class="muted">Líneas/impuestos avanzados quedan diferidos; P4 usa cabecera foundation controlada.</p>`
})
export class PurchaseTaxLineFormComponent {}
